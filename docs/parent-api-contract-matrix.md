# Parent API Contract Matrix

Legacy native apps must keep working behaviorally through the modern parent API/portal.

Parent web PWA status:

- `/parent/login` now uses `/api/parent/login`, persists the parent JWT and child id in browser storage, and keeps failed login responses in the legacy `status: false` shape.
- `/parent` now consumes the daily detailed, finance, absence, grouped messages, message thread detail/reply, notifications, alarm feeds, push-token, food calendar, holiday calendar, and send-message endpoints as the first parent-facing web shell.
- Direct `/ws/*.php` routes and directory-prefixed `/:legacyPath/ws/*.php` aliases now exist for the active legacy native endpoints and delegate into the restored `/api/parent/*` contracts, so iOS/Android builds that still call literal PHP paths no longer require URL rewrites.
- Native parser field coverage is now codified in `src/scripts/verify-parent-native-parser-fields.ts` across the active iOS/Android parent feeds, including the iOS string-id login contract, and credentialed `/ws/login.php` -> `/ws/notifications_master.php` E2E is codified in `src/scripts/verify-parent-credentialed-native-e2e.ts`; remaining contract work is broader production-data/native-device acceptance.

| Legacy Endpoint | Request Fields | Modern Endpoint | Status | Referenced By iOS | Referenced By Android |
| --- | --- | --- | --- | --- | --- |
| master.php | p | /master.php | restored - iOS nursery bootstrap now returns legacy `t_garderies` rows from migrated `LegacyGarderieRegistry` with `gid`, `garderie_name`, `garderie_alias`, `user_manage_db`, `current_db`, and `path`; empty/dev registry reads fall back to a parser-safe current-app row; returned `path` values are supported by `/:legacyPath/ws/*.php` aliases | yes | no |
| ws/absence.php | usites | /api/parent/absence/[childId] | partial - used by /parent PWA; unauth native POST `usites`, header/count, string report ids, iOS/Android force-read string fields, Android-safe absent-from/to fallbacks, raw `t_absent_report` fields, migrated legacy ids, and shared legacy empty-header fallback on native POST/database/auth-lookup failures restored; credentialed native E2E remains | yes | yes |
| ws/birthdays_alarms.php | pid | /api/parent/alarms/birthdays | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string id/status/datetime/href fields, `email-birthday-msg` detail rendering, level-0 filtering, and native POST database-connection fallback restored; verify parent delivery push | no | no |
| ws/daily.php | usites | /api/parent/daily/[childId] | partial - unauth native POST `usites`, header/count, string report ids, raw daily field names, legacy `present` fallback for modern submitted reports, flattened fever pairs `0`-`9`, first milk row, food names, and legacy empty-header fallback on database/auth-lookup connection failures restored; credentialed native E2E remains | no | yes |
| ws/events_alarms.php | pid | /api/parent/alarms/events | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string event field names including `active `, multi-branch filtering, and native POST database-connection fallback restored; verify parent delivery push | no | no |
| ws/finance.php | usites | /api/parent/finance/[childId] | partial - used by /parent PWA; unauth native POST `usites`, header/count, iOS/Android force-read string fields, legacy target/method fallback casing, raw `t_payments` fields, migrated legacy ids, active-payment filtering, and shared legacy empty-header fallback on native POST/database/auth-lookup failures restored; credentialed native E2E remains | yes | yes |
| ws/foodcalendar.php | usites | /api/parent/calendar/food | partial - used by /parent PWA; unauth native POST `usites`, header `branch_id`, iOS force-read string fields, Android date/grouping-safe rows, raw `t_food_calendar` date/dessert fields, breakfast/lunch names, migrated legacy ids, and shared legacy empty-header fallback on native POST/database/auth-lookup failures restored; credentialed native E2E remains | yes | yes |
| ws/general_alarms.php |  | /api/parent/alarms/general | partial - unauth legacy no-pid feed remapped to migrated `t_alarms` rows with parser-safe empty header fallback and string legacy `aid`/`hid`/status/datetime/href fields; database-connection failures now degrade to the legacy empty header instead of 500; verify production exposure expectations | no | no |
| ws/holcalendar.php | usites | /api/parent/calendar/holidays | partial - used by /parent PWA; unauth native POST `usites`, active-only rows, iOS force-read and Android date-safe description/date fields, repeated-date current-year adjustment, and shared legacy empty-header fallback on native POST/database/auth-lookup failures restored; credentialed native E2E remains | yes | yes |
| ws/holcalendarOLD.php | usites | /api/parent/calendar/holidays | partial - unauth native POST `usites` and active-only description/date array restored through the same holiday endpoint, including legacy empty-header fallback on database-connection failures | no | no |
| ws/insurance_alarms.php | pid | /api/parent/alarms/insurance | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string id/status/datetime/date/href fields, newline-cleaned details, and native POST database-connection fallback restored; verify parent delivery push | no | no |
| ws/login.php | name, pass | /api/parent/login | partial - JSON/form login restored with reusable legacy success/failure contract, exact default failed fields, string `id`/`usites` on successful login for the iOS parser, parser-safe modern id fallback, persisted token, parent report URL encoding, md5-prefixed legacy password verification, legacy-shaped rate-limit/database fallbacks, and modern `childId` for the PWA; credentialed `/ws/login.php` route-handler E2E over a known active parent account now validates successful string `id`/`usites`, token/report URL, and modern fallback fields | yes | yes |
| ws/medicine_alarms.php | pid | /api/parent/alarms/medicine | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string id/status/datetime/href fields, newline-cleaned details, and native POST database-connection fallback restored; verify parent delivery push | no | no |
| ws/message.php | usites (thread_id) | /api/parent/messages/thread/[threadId] | partial - unauth native POST `usites` thread open, legacy numeric-key thread object, POST thread id override, SQL datetime strings, legacy sender values, string field guardrails, legacy no-read-reset behavior for unauthenticated opens, authenticated PWA read-on-open, runtime fan-out dedupe, and database-connection empty-thread fallback restored; verify credentialed thread open | no | no |
| ws/messages.php | usites, pid | /api/parent/messages/[childId] | partial - direct legacy route restored as a parser-safe message-list alias because iOS `GetMessages` calls it; backup PHP file is a stale daily-report copy, and active list parity remains covered by the `messagesList.php` contract | yes | no |
| ws/messagesList.php | usites | /api/parent/messages/[childId] | partial - unauth native POST `usites`, header/count, numeric thread ids, SQL datetime strings, always-ellipsis last message, original sender, recipient-anchored thread grouping, latest whole-thread preview, string field guardrails, and database-connection empty-header fallback restored; verify credentialed native parser tolerance | no | no |
| ws/missingReports_alarms.php | pid | /api/parent/alarms/medical | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string migrated medical alarm fields, legacy-absent status/href fields, newline-cleaned details, and native POST database-connection fallback restored; verify encrypted deep-link behavior | no | no |
| ws/newassessment_alarms.php | pid | /api/parent/alarms/assessments | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string migrated and modern non-draft `new_assessment` marker ids/child ids/datetimes, newline-cleaned `email-assessment-msg` template rendering, and native POST database-connection fallback restored; verify external parent/mobile delivery and credentialed parser tolerance | no | no |
| ws/newdaily.php | usites | /api/parent/daily/[childId]/detailed | partial - used by /parent PWA; unauth native POST `usites`, iOS force-read key/type guardrails, string report ids, legacy `present` fallback for modern submitted reports, mapped dessert portion, fever/milk arrays, `takenmeds_Arr` name resolution, and legacy empty-header fallback on database/auth-lookup connection failures restored; credentialed native E2E remains | yes | no |
| ws/notifications.php | usites | /api/parent/notifications/[childId] | partial - direct legacy route restored as an alias for older iOS `notifications` operation names; payload matches the parser-safe `notifications_master.php` contract | yes | no |
| ws/notifications_master.php | usites | /api/parent/notifications/[childId] | partial - dynamic `notifications_nature` ordering/names/active flags, unauth native POST `usites`, authenticated PWA guard, eleven `notificationN` groups for iOS, string group/detail guardrails, exact production fallback natures, birthday parent rows, assessment parent rows, mapped alarm/event/detail groups, and database-connection empty eleven-group fallback restored; credentialed E2E now validates bearer + `usites` flow and all eleven iOS force-read groups/detail string fields | yes | no |
| ws/payments_alarms.php | pid | /api/parent/alarms/payments | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string id/status/datetime/href fields, newline-cleaned details, and native POST database-connection fallback restored; verify parent delivery push | no | no |
| ws/pnotifications.php | cid, del, os, show, token | /api/parent/push-token | partial - JSON/form registration, unauth native `cid` writes, authenticated PWA writes, global legacy delete/show fallback, parent-scoped authenticated show, exact legacy result strings including `Missing Paramaters`, parser-safe show rows, legacy OS mapping, soft-delete, and database-connection fallbacks restored; verify credentialed DB write and external push delivery | yes | no |
| ws/sendMessage.php | message, subject, threadid, to, usites | /api/parent/messages | partial - unauth native POST `usites`, JSON/form send, legacy child id matching, `Message Sent`/`Failed` feedback, numeric `threadid` response, database-connection failed-send fallback, mobile-message nature/href provenance, replies to numeric/UUID threads, and admin/teacher recipient fan-out restored; verify credentialed send/reply | no | no |
| ws/vaccinations_alarms.php | pid | /api/parent/alarms/vaccinations | partial - unauth native POST `pid`, parser-safe legacy empty header fallback, string id/status/datetime/href fields, newline-cleaned details, and native POST database-connection fallback restored; verify parent delivery push | no | no |
